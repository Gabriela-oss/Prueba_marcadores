<%= form_with(model: musicmark, local: false) do |form| %>
  <% if musicmark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(musicmark.errors.count, "error") %> prohibited this musicmark from being saved:</h2>

      <ul>
        <% musicmark.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :band %>
    <%= form.text_field :band %>
  </div>

  <div class="field">
    <%= form.label :album %>
    <%= form.text_field :album %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%#= form.text_field :category_id %>
    <%= form.select :category_id, Category.pluck(:name, :id)%> 
  </div>

  <div class="field">
    <%= form.label :kind_id %>
    <%#= form.text_field :kind_id %>
    <%= form.select :kind_id, Kind.pluck(:name, :id)%>
  </div>

  <div class="actions">
    <%= form.submit data: {confirm: 'Are you sure to create this musicmark?' } %>
  </div>
<% end %>
